
  # SisCras

  This is a code bundle for SisCras. The original project is available at https://www.figma.com/design/Zyy8DYOiAotS8R98FDYqhn/SisCras.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  